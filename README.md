# Fitness Data Ingestion Project

## Description
This project implements a data ingestion pipeline for fitness tracker datasets.

## Features
- Reads CSV, JSON, and GPX files
- Handles missing values
- Standardizes timestamps
- Generates summary statistics
- Handles empty files gracefully

## How to Run

```bash
pip install pandas gpxpy
python ingest.py
